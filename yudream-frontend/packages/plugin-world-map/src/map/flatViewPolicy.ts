/** Flat overview needs enough camera distance to preserve its 2 km default height and shared views. */
export const FLAT_VIEW_MIN_DISTANCE = 1
export const FLAT_VIEW_MAX_DISTANCE = 8_000
/** Starts an overview close enough to inspect terrain before the user pans or zooms. */
export const FLAT_VIEW_DEFAULT_ZOOM = 2
