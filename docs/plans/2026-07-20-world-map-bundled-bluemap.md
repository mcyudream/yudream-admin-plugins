# World Map Bundled BlueMap Runtime Plan

1. Add failing tests for bundled runtime resolution and classpath extraction.
2. Add the pinned CLI and a placeholder-complete template under plugin resources.
3. Implement bundled runtime extraction and make it the default configuration source.
4. Gate legacy external settings behind an explicit opt-in flag and update operations documentation.
5. Run targeted tests, package the plugin, and inspect the JAR contents.
