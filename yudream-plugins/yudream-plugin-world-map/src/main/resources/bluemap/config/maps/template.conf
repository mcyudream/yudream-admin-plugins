world: "${world}"
dimension: "${dimension}"
name: "${name}"
storage: "file"
enable-hires: true
render-edges: true
# Render imported or previously-unloaded chunks even when their light arrays were not saved.
# This prevents BlueMap from silently omitting most of an uploaded world.
ignore-missing-light-data: true
